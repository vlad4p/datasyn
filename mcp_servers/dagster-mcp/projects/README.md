# Scaffolded Dagster projects live here.

Each subfolder is normally one Dagster code location, generated by `dagster_create_project`.

**Tracked project:** `datasynk/` (whitelisted in `.gitignore`). For new ad-hoc scaffolds, add `!your_project/` and `!your_project/**` the same way.
